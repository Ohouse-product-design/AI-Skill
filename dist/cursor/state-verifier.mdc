---
description: "UI 상태 누락과 엣지케이스를 탐지하는 스킬. \"상태 체크\", \"엣지케이스 뽑아줘\", \"QA 전 체크\" 등의 표현에 반응. Figma URL이나 화면 이미지를 인풋으로 받아 Loading/Empty/Error/Success/Disabled 상태 누락을 점검하고 체크리스트를 생성한다."
alwaysApply: false
---


# State-verifier 스킬

Figma 화면 구조를 읽고 UI 상태 누락과 엣지케이스를 탐지하여 체크리스트를 생성하는 팀 공용 스킬.


## Step 1 — 인풋 확인 및 분기

Figma URL 또는 이미지와 함께 아래 맥락 정보가 있으면 같이 받는다.
없으면 Figma 구조만으로 진행하되, 탐지 정확도가 낮아질 수 있음을 안내한다.

```
함께 주시면 분석 품질이 올라가요 (선택)
• 이 화면의 핵심 정책이나 결정사항
• PRD / 1Pager 요약
• 이전 대화에서 확정된 내용
```

### A. 단일 프레임 URL
2. 주요 화면 프레임 목록 표시:

```
이 섹션에 {N}개 화면이 있습니다.

1. [프레임명] — node-id: XXXX:XXXX
2. [프레임명] — node-id: XXXX:XXXX
...

전체 처리할까요, 특정 화면만 할까요?
전체면 "전체", 특정 화면이면 번호를 말씀해주세요.
```

